# Informe para David — Serie 5.26 (2026-09-11)

Tu «ok» a la recomendación de modelos, un commit, 556 tests en verde.

## Las dos ruedas (en tu panel, producción)
| Rueda | Antes | Ahora |
|---|---|---|
| Clasificador factual/opinión | Qwen 3.8 Max / Sonnet 4.6 | **Qwen 3.7 Plus** / Sonnet 4.6 |
| Orden de categorías | Qwen 3.7 Plus / Sonnet 4.6 | **Qwen 3.8 Flash** / Haiku 4.5 |

Son ajustes de tu panel de modelos (`/panel/modelos/`); los cambié yo con tu orden y puedes
revertirlos allí en cualquier momento. Los valores de fábrica del código quedan alineados para
instalaciones nuevas (categorías → Flash con Haiku de respaldo; el clasificador ya era Plus de fábrica).

**Nota tras el despliegue (13:45)**: al verificar producción, las ruedas NO estaban como las dejé.
El registro de auditoría muestra dos guardados de `/panel/modelos/` por tu usuario a las 13:25 y
13:34, posteriores a mi cambio de las 13:20. Estado real ahora: clasificador **Qwen 3.8 Flash** /
Sonnet 4.6 y categorías Qwen 3.8 Flash / **Sonnet 4.6**. El panel es tuyo, así que lo dejo tal cual;
si querías Plus en el clasificador y Haiku de respaldo en categorías, vuelve a girarlas tú (o dímelo).
Ojo: el formulario guarda TODAS las ruedas con lo que muestra en pantalla; si la página estaba
abierta desde antes de un cambio ajeno, al guardar lo pisa.

## 5.26-A · Miniparche
1. **Precios reales de Qwen en el catálogo** (ficha de Alibaba, región Singapur = la de
   `dashscope-intl`, leída hoy): Flash 0,15 / 0,47 $ por millón (antes 0,11 / 0,80 estimados) y
   Plus 0,40 / 1,60 $ (antes 0,39 / 2,34). El libro de gastos sobreestimaba la salida de Plus un
   45 %; a partir de ahora apunta lo que Alibaba cobra. Max sigue en 2 / 6 $ (ya era el real, de tu factura).
2. **El libro apunta QUÉ MODELO gastó**: columna nueva `model` en cada apunte (migración
   `analysis/0023`), rellenada por las llamadas a Qwen y a Claude (tokens y búsquedas). Los apuntes
   de GPU, audio y correo la dejan vacía porque no son de un modelo.
3. **Panel Gastos**: filtro «Modelo contiene», columna Modelo en la tabla y en el CSV, y un
   resumen «Por modelo» con el porcentaje de cada uno (clic en el nombre = filtra). La próxima vez
   que preguntes cuánto se llevó Max, saldrá de datos y no de estimaciones. Los apuntes anteriores
   a hoy no tienen modelo (no se guardaba).

## Verificado en vivo antes de recomendar
- Qwen 3.8 Flash y Qwen 3.7 Plus **buscan en internet** con la clave de producción (una pregunta
  de actualidad cada uno, fuentes devueltas: 9 y 12; coste total ~2 céntimos, apuntados).
- Gasto real del mes a las 12:00: 23,31 € (19,57 € del día 6, la migración a Qwen con relanzados;
  0 € desde el día 8). Un análisis con la configuración actual costó ~1,25 € el día 7 (30 % búsquedas).
- La palanca real de coste es el número de búsquedas por afirmación (tope 3, en tu panel), no el modelo.

## Pendiente tuyo (sin cambios)
Donación de prueba de 1 € desde otra cuenta (o tarjeta como invitado) para estrenar el IPN ·
revocar el token de GitHub con todos los permisos y crear uno con repo+workflow+write:packages ·
borrar las etiquetas viejas de ghcr (4.4-J, 4.4-J-p4, 4.4-J-slim) · decisión Google
(`wiki_index_people`) · si quieres el nombre «esestocierto?» en PayPal, cuenta Business.
