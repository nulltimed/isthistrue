# Informe — Pase 4.2 (2026-08-15): el pase grande

**Formato nuevo (parche git sobre main real) · Commit final `802164c` · CI verde: run 31906789063 · Tests 42/42**

## Resultado

Los bloques A1-A6, C1-C6, D1-D4, E1, F1, G1 y H1-H9 están **EN PRODUCCIÓN**: relegación solo
manual, SearXNG cableado a los veredictos con fuentes (y ruidoso al fallar), hilo unificado
foro↔análisis, frases completas por hablante, campana viva con notificaciones, suscripciones
por post, Trending, logo ×2 por dominio, Mi cuenta rediseñado, títulos reales de vídeo,
buscador adaptado, moderación de sensibles, mensajes privados opt-in y más.

## El formato parche funcionó — con matices

`git apply` limpio sobre `c5b5c13` (Fable clonó el main real: la regla de tu §10 al fin
ejecutada del todo). El CI hizo de portero y **cazó 6 fallos en el primer run**; arreglados
por el operador (commit `802164c`):

1. **BUG REAL — el slug de machina** (rompía todo C4): `Topic.save()` de machina REGENERA el
   slug desde el subject y pisaba el `post-<pk>` del que depende el hilo unificado. Forzado
   con `update()` tras crear el topic (y tras cada save en move_topic).
2. **BUG REAL — Trending sin aviso al autor**: `notify_post_event` solo incluía al autor en
   'analysis'; D4 exige "aviso a autor/suscritos". Corregido.
3-5. **Tests**: ALLOWED_HOSTS de test sin los dominios (logo-por-dominio moría en
   DisallowedHost); `MOCK_AGENTS=False` a nivel de clase hacía que upsert_claim llamara a la
   API real (movido al método del 403); y el test antiguo de relegación seguía esperando la
   semántica pre-A2 (el parche cambió el comportamiento sin actualizarlo).

## Incidencia de despliegue nueva (documentada para siempre)

Las migraciones de H8 añaden campos a User, y el `ensure_superuser` del arranque del web los
consulta ANTES de que migrate pueda correr → el web no arranca. **Orden nuevo del ritual
cuando un pase migra el modelo User**: `docker compose run --rm web python manage.py migrate`
con el web parado, y DESPUÉS levantar. (Aplicado en espejo y producción.)
Nota: en el espejo, el `--force-recreate searxng` de la guía no aplica (el espejo no tiene
searxng por diseño); en producción sí se ejecutó.

## Verificaciones clave

- **C1 (la crítica)**: `searxng:8080/search?format=json` → **200 con 21 resultados** ✓
- **Re-veredictos pendientes (dry-run)**: **0 posts** con claims sin fuentes. El marcado manual
  de los claims del 2026-08-15 (`sources_ok=False`) queda a tu confirmación — no se tocó nada.
- `backfill_titles` ejecutado en espejo y producción ("Hecho.").
- C6: escierto→logo-es · isthistrue→logo-en ✓ · C3/C4: foro con piel + hilo unificado ✓
- E1/H: Mi cuenta con rejilla nueva, buzón MP (200), panel moderadores (200), iconos SVG ✓
- Smoke: CSS 200 (18.946 B, >5 KB) + masthead + portadas 200 en los 3 dominios · logs limpios.

## PENDIENTE DAVID

1. Confirmar (o no) el marcado manual de claims del 15-08 para re-veredicto con fuentes
   (~0,07 €/post; comando listo, pasa por try_spend).
2. Paseo visual: hilo unificado, campana (el permiso del navegador se pide SOLO al clicar),
   suscripciones 🔔, Mi cuenta nuevo, MP (buzón OFF por defecto — actívalo en Mi cuenta).
3. El pase 4.3 (identificación participativa de hablantes) queda anunciado — el hueco visual ya existe.
