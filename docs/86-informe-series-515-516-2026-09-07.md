# Informe de las series 5.15 y 5.16 — el curl muere y el DRM confiesa (2026-09-07)

## 5.15 — Verificación de pedidos contra la API de PayPal

David preguntó cómo se colaba una donación falsa; la respuesta (un curl con
pedido inventado, porque el servidor se creía al navegador) está en el chat y
en docs/85. Con su OK y sus credenciales REST en el .env:

- `apps/analysis/paypal_check.py`: OAuth + `GET /v2/checkout/orders/<id>` —
  el pedido debe EXISTIR, estar COMPLETED y cubrir el importe en EUR.
- `donation_capture`: con credenciales, un pedido no verificable muere con
  400 + AuditLog ANTES de anotar nada o lanzar apadrinamientos. Sin
  credenciales, circuito anterior (reversible vaciando las líneas del .env).
- El SDK del banner usa EL MISMO client-id del .env (PayPal solo deja
  consultar pedidos a la app que los creó); el viejo queda de respaldo en el
  código. PENDIENTE de David: donación real de 1 € para estrenar el circuito.

## 5.16 — El DRM de Spotify confiesa en vez de dejar zombis

El post 6 (podcast de David) llevaba horas en CHEAP_RUNNING con 0 frases:
yt-dlp reventó con «[DRM] The requested site is known to use DRM protection»
y el try/finally sin except dejó el estado colgado.

- Candado: el DRM pasa el post a **FAILED** con aviso al autor, sin
  reintentos ciegos (el DRM no se arregla reintentando).
- Realidad estructural: **los episodios de Spotify no se pueden analizar**
  (audio cifrado). Opciones dichas a David: postear la versión de YouTube, o
  construir ingesta por RSS (feeds públicos con MP3) — decisión suya.
- El post 6 marcado FAILED a mano; su reserva de presupuesto quedó gastada
  (fleco: devolver la reserva al fallar — anotado).

## Además

Segunda salida del relanzamiento profundo global (5.13-B) tras desplegar el
tren 5.14-5.16: esta vez con los ojos funcionales (selector bv* probado).
Resultado en el addendum cuando termine.
