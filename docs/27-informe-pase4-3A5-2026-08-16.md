# Informe — Pase 4.3-A.5 (2026-08-16): la conversación en orden y el registro imposible de pasar por alto

**Parche sobre 4.3-A.4 · [CI verde](https://github.com/nulltimed/isthistrue/actions/runs/31968014480) 62/62 A LA PRIMERA · Commit `4dde63c` · Sin migraciones · Cero arreglos del operador**

## Resultado — EN PRODUCCIÓN

- **O1 · El post 4 ya se lee seguido, y sin reanalizarlo.** Confirmado en producción: los
  tiempos salen 5,01 → 9,25 → 13,61 → 17,25 → 22,25… en orden. El diagnóstico de Fable era
  exacto: al añadir contadores con `annotate`, PostgreSQL agrupa y **descarta el orden por
  defecto del modelo**, así que la base devolvía las frases por orden de inserción. Una línea
  (`order_by('start_seconds','pk')`) y arreglado — **tus datos siempre estuvieron bien**.
- **O2 · Intervención activa en negro con letra blanca** (sustituye al ámbar), y la ficha del
  hablante actual, igual.
- **O3 · «↻ Reanalizar con IA»**, solo para moderadores y con confirmación: borra la
  transcripción, deja el post en cola y relanza el análisis (pasa por el candado de
  presupuesto). Para cuando un post SÍ venga mal de origen.
- **O4 · El registro, destacado** (tu petición): sección propia arriba del panel de ajustes,
  con recuadro **verde si abierto / rojo si cerrado** y el estado escrito en palabras con su
  semáforo. Verificado en el espejo: 🟢 y `reg-open`.

## Verificación

Espejo: 62/62 · tiempos en orden ascendente · botón de reanalizar presente como mod ·
panel con `.reg-gate`, semáforo verde y clase `reg-open`.
Producción: **post 4 en orden cronológico** · CSS 200 (24.418 B) + masthead + portadas 200 en
los 3 dominios · logs limpios · GitHub = producción = espejo.

## Calidad de la entrega

Segundo pase consecutivo con CI verde a la primera y sin un solo arreglo del operador. El
diagnóstico de O1 (GROUP BY vs `ordering` del Meta) es de los que ahorran horas: la tentación
era culpar a los datos o al análisis, y era una línea de ORM.

## Tu paseo

1. Abre el post 4: la conversación se lee de arriba abajo, en orden.
2. Dale al play: la frase que suena se pone **negra con letra blanca** y salta al cambiar de
   hablante, con su ficha marcada a la izquierda.
3. Panel → Ajustes: arriba del todo, el recuadro del registro con su semáforo. Apágalo y
   míralo ponerse rojo (y el registro cerrarse de verdad); enciéndelo y vuelve el verde.
4. Si algún post viniera mal de origen: «↻ Reanalizar con IA» en las acciones de moderador.

## PENDIENTE DAVID

PayPal: cancelar el plan de suscripción antiguo `P-3K…BAI` en su panel.
