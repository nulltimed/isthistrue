# Informe para David — Serie 5.25 (2026-09-09, madrugada)

Dos reportes tuyos, un commit, 547 tests en verde.

## A · «Vídeos donde aparece», al principio de la ficha de persona
La sección pasa de la cola de la página a justo debajo de la cabecera y del aviso legal, antes de
las estadísticas y de las afirmaciones por color. Test que fija el orden.

## B · El mensaje «Servicio pausado por el administrador. Trabajando en ello...»
- **No era PayPal ni nuestra web.** Pulsaste «Donar» a las 02:19:20, justo en la ventana de ~40 s en
  que producción se recreaba para desplegar el 5.24-E (`down → up`). El servidor devolvió 502 y el
  Nginx del host sirvió su **página de emergencia** (`/var/www/isthistrue-panic/panic.html`,
  creada el 05-08 en el primer despliegue), que llevaba ese texto. Lo prueba el registro de Nginx:
  `02:19:20 POST /donaciones/iniciar/ 502` y, 50 s después, `02:20:11 POST /donaciones/iniciar/ 302`
  hacia tu página de PayPal.
- **Texto nuevo** (solo el HTML de la página; la configuración de Nginx no se toca): logo en letra
  de máquina, «Estamos actualizando la web. Vuelve en un minuto: esta página se recarga sola», su
  versión en inglés, y recarga automática cada 15 s para que el visitante no tenga que hacer nada.
  Copia versionada en `nginx/panic.html`. Si prefieres otra frase, dímela.
- **Propuesta para que no vuelva a pasar**: desplegar sin `down` (solo `up --build -d`, que recrea
  cada contenedor en su sitio) reduce el corte de ~40 s a unos segundos por contenedor. El ritual lo
  fija CLAUDE.md, así que lo cambio solo si me dices que sí.

## Donación de prueba
El botón funciona (tu segundo intento a las 02:20 ya te llevó a PayPal). Cuando hagas la donación
de 1 €, avísame y compruebo el IPN, el apunte verificado y el depósito.
