# Informe — Pase 3.9 (2026-08-13): primer post posible y obvio + idioma por navegador

> Ritual canónico. Runs verdes: [pase](https://github.com/nulltimed/isthistrue/actions/runs/31756859762) · [alias](https://github.com/nulltimed/isthistrue/actions/runs/31757279306)

## Resultado

**EN PRODUCCIÓN** (commit 37496f6): CTA-héroe en portada (anónimo → registro; logueado → analizar),
píldora "🔍 Analizar un vídeo" en la nav, formulario de análisis en tarjeta clara con validación
de plataforma también en servidor, e **idioma por navegador** con selector ES·EN fijo arriba a la
derecha que persiste por cookie. El logo sigue al idioma (escierto./isthistrue.).

## Cambios de fondo

- `HostLanguageMiddleware` ELIMINADO: el idioma ya no lo impone el dominio; orden nuevo =
  cookie del selector → Accept-Language del navegador → 'es'. (Verifiqué antes con grep que
  nada usaba su `request.site_section`; el LocaleMiddleware ya estaba bien colocado.)
- `submit`: puerta abierta según diseño — solo login + **email verificado** (añadido: faltaba)
  + cuota diaria. Enlaces no soportados se RECHAZAN con mensaje claro (antes caían a
  "tarjeta-enlace"): regex de plataformas en servidor, el required del navegador es cortesía.
- Endpoint `i18n/setlang` + `LANGUAGES` alineado a la guía ('Español').
- Alias del operador: `/analizar/` sirve la misma vista que `/submit/` (la guía nombraba esa URL).

## Checklist 74-79 (verificado espejo + producción)

- **74/75.** ✔ CTA anónimo → `/accounts/register/` (comprobado en el HTML de producción);
  logueado → botón grande a analizar + píldora en nav. *Tu parte del 74: con tu cuenta,
  publica un YouTube desde el botón grande y ve cómo entra en cola.*
- **76.** ✔ Un tuit → error rojo "plataforma soportada", sin crear post.
- **77.** ✔ Navegador en inglés → web en EN con logo isthistrue.; selector a ES/EN → 302 y
  **persiste tras recargar** (probado con cookie real en producción, ida y vuelta).
- **78.** ✔ CSS 200 con 10.186 B (>5 KB) en los 3 dominios; masthead ✓; portadas 200.
- **79.** ✔ Los 3 links del favicon siguen en el head.
- Tests 21/21; logs limpios.

## Nota para ti

La guía de este pase ya venía con mis notas §14/§15 interiorizadas (umbral >5 KB, cadena del
command intacta, decisión delegada con plan B). El circuito guía→operador→addendum funciona.

**Backups (docs/11): siguen pendientes.** Con CTA nuevo y usuarios entrando, cada día sin
copia es una apuesta.
