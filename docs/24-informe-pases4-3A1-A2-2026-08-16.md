# Informe — Pases 4.3-A.1 y 4.3-A.2 (2026-08-16), aplicados en orden

**Dos ciclos completos del ritual, uno por pase · CI: [A.1 verde](https://github.com/nulltimed/isthistrue/actions/runs/31939552240) (51/51) · [A.2 verde](https://github.com/nulltimed/isthistrue/actions/runs/31946443548) (53/53) · Commit final `802a4d9`**

## Pase 4.3-A.1 — el hotfix urgente (EN PRODUCCIÓN)

- **K2, el importante**: fin de la recarga infinita cada 4-5 s. La página ya no se recarga
  jamás en posts terminados; con un análisis en curso, UNA transición y silencio.
- **K3**: candidatos automáticos de nombre desactivados + **migración de PURGA intencional**,
  verificada con lupa: espejo 1 auto eliminada / 1 confirmada conservada; producción
  **3 candidatos-basura eliminados** (adiós «Edición Daniel Castresana»), 0 confirmadas
  afectadas. Proponer un nombre ahora busca la foto de Wikipedia al proponer.
- **K1**: fuera los tres comentarios de plantilla que se veían como texto. **K5**: test de
  guardia que rompe el CI si alguien reintroduce un `{# #}` multilínea — la lección hecha candado.
- **K4**: rejilla a todo el ancho (vídeo protagonista). **K6**: toggle registro
  abierto/cerrado en el panel — probado en espejo: cerrado → 302 con aviso; reabierto ✓.

## Pase 4.3-A.2 — cero recargas (EN PRODUCCIÓN)

- **L3**: `HX-Refresh` EXTINTO en todo el árbol (grep=0, con test). El contenido nuevo entra
  EN EL SITIO por eventos (`isttBodyRefresh` intercambia el cuerpo sin tocar el scroll).
- **L4**: bocadillos de la casa en TODAS las páginas (borde ámbar, arriba-derecha, 5 s,
  pausa al hover, ✕, clic→foco; >5 avisos en 3 s → uno solo «×N novedades»).
- **L5**: la campana también canta bocadillo. **L6**: sonido OFF por defecto (quiz 78),
  toggle en Mi cuenta, WebAudio sin archivos; el atributo `data-toast-sound` solo se emite
  con la preferencia activa (verificado: con `d` en OFF no aparece — diseño correcto).
- **L2**: `_post_context()` compartido página/fragmento; `/post/<pk>/fragmento/cuerpo/` → 200.

## Ritual e incidencias

**Cero arreglos del operador en ambos pases** — primera vez que dos parches seguidos pasan
CI, espejo y producción sin tocar una línea. El circuito Fable→operador ha madurado.

## Smoke final (candado, producción)

```
escierto:   CSS 200 21859B | masthead: 1 | portada: 200 | toasts.js ✓
isthistrue: CSS 200 21859B | masthead: 1 | portada: 200 | toasts.js ✓
wikitrue:   CSS 200 21859B | masthead: 1 | portada: 200 | toasts.js ✓
toasts.js: 200 (4,4 KB) · logs limpios · GitHub = producción = espejo
```

## Tu paseo

1. Abre un post analizado: NADA se recarga ya (mira la pestaña: sin parpadeos).
2. Postea un vídeo y déjalo analizándose: la transcripción APARECE en el sitio con su
   bocadillo, sin mover tu scroll.
3. Desde otro navegador escribe en el hilo: mensaje + bocadillo clicable que baja a #hilo.
4. Mi cuenta → Notificaciones → activa el sonido, clica una vez en la página y provoca un
   aviso: «pop» suave. Apágalo: mudo.
5. El debate 23J: «¿Quién habla?» limpio; propón tú un nombre y mira la foto de Wikipedia.
6. Panel → settings: `registration_open` a 0 cierra el registro al instante (y a 1 lo abre).

## PENDIENTE DAVID (recordatorio de la guía A.1)

Cancelar el plan de suscripción antiguo en el panel de PayPal (fuera del código; sigue vivo allí).
