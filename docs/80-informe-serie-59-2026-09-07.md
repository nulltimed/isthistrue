# Informe de la serie 5.9 — el Foro en categorías de solo-enlaces (2026-09-07)

**Commit**: `8dac105`. Orden de David:

«En la página "Foro" deben mostrarse, en categorías: los posts más nuevos,
los posts más comentados, los posts analizados en profundidad ordenados por
más votos, y Off-Topic con los posts más nuevos y los posts más comentados.
Sin mostrar comentarios, sólo el enlace al post».

## Qué se hizo

- `/foro/` reescrito (supersede los bloques-con-mensajes del 5.1-B): el
  buscador con autocompletado arriba, y debajo **cinco categorías de solo
  enlaces** — Principal: «Los más nuevos», «Los más comentados» (💬 n) y
  «Analizados en profundidad, por votos» (▲ n); Off-Topic: «Los más nuevos»
  y «Los más comentados». Ningún cuerpo de mensaje.
- **Interpretación anotada**: «analizados en profundidad» = análisis COMPLETO
  con veredictos (status DONE), ordenados por votos totales. Si David quiso
  otra cosa (p. ej. solo los re-escaneados premium), es un filtro de una
  línea en `foro_home` — está comentado en el código.
- Test del 5.1-B reescrito como supersedido + `Parche59_ForoEnCategorias`
  (cinco categorías, orden por votos dentro de profundos, off-topic enlazado,
  sin `thread-body`).
