# Informe — Pase 4.1 (2026-08-15): diarización, deno, PayPal opción B, admin vestido

> Desarrollado POR EL OPERADOR según pase-4.1-orden-de-trabajo.md (sin ZIP).
> Ritual canónico. CI verde: https://github.com/nulltimed/isthistrue/actions/runs/31886159268 · Commit 5d15111.
> Versión PDF entregada a David; detalles técnicos completos en docs/18 (README de operador).

## Aceptación (todo APROBADO, espejo + producción)

- **B1**: `import pyannote.audio` OK en la imagen (torch/torchaudio 2.2.2+cpu + numpy 1.26.4 + pyannote 3.1.1 fijados, verificación en build); sin token o con fallo → WARNING con causa exacta (fin del except mudo, regla 5.7; tests con pyannote mockeado); HF_HOME=/hfcache persistente (excluido del backup: re-descargable); solo SPEAKER_XX, cero huellas de voz.
- **B2**: deno 2.1.4 en la imagen; yt-dlp sin el WARNING de JS runtime.
- **B3**: suscripción retirada al 100% (grep limpio); selector 5/10/otra (mín 1 €) + SDK capture EUR una vez + createOrder con la cantidad; noscript → paypal_url; panel rechaza importes ≤ 0 (test).
- **B4**: admin y su login con la piel de la web (logo por idioma, volver a la web, favicons ×3, paleta, tarjeta); cero cambios funcionales.
- Tests 25/25 · sin migraciones · smoke de estáticos verde (CSS 200, 11.197 B) · logs limpios.

## Causa raíz B1 (doble)

1. `torchaudio.AudioMetaData` retirada del torchaudio moderno; pyannote 3.x aún la referencia.
2. torch 2.2.2 compilado contra NumPy 1.x vs NumPy 2.5 resuelto por pip — cazada por el candado de build nuevo, no por producción.

## PENDIENTE DAVID

1. Cancelar el plan de suscripción antiguo en el panel de PayPal (las renovaciones de posibles suscriptores seguirían).
2. `paypal_url` en /panel/settings/ si sigue vacío (destino del fallback sin JS).
3. Prueba visual del selector y, si quieres, 1 € real de punta a punta.
4. El pase 4.0 no ha llegado: al llegar, ojo al roce en base.html (invariantes 5.12).
